<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <target>System.out</target>
        <encoder class="ch.qos.logback.core.encoder.LayoutWrappingEncoder">
            <layout class="it.pagopa.interop.commons.logging.LoggerLayout"/>
        </encoder>
    </appender>

    <appender name="ASYNC" class="ch.qos.logback.classic.AsyncAppender">
        <queueSize>8192</queueSize>
        <neverBlock>true</neverBlock>
        <appender-ref ref="CONSOLE" />
    </appender>

    <root name="application" level="INFO">
        <appender-ref ref="ASYNC"/>
    </root>

    <logger name="oshi.hardware.platform.mac.MacHWDiskStore" level="OFF"/>
    <logger name="akka.persistence.typed.internal.EventSourcedBehaviorImpl" level="WARN"/>

</configuration>
